import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.code.financeBuddy',
  appName: 'finance-buddy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
